# include<stdio.h>


int main (void) 
{

	/*
	 * 1. A pointer to 'void' is used to hold any type of pointer but can not 
	 *    be used to dereference itself.
	 * */
	void *p;

	/*
	 * 2. 
	 * */
	double *dp, atof(char *);


	return 0;
}
